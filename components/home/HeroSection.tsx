// HeroSection: full-width, /public/photos-to-use/hero-section.png background
// Left side dark gradient overlay via CSS (left-to-right on desktop, top-to-bottom on mobile)
// Content: headline, subtitle, 5 feature icons row, 2 CTA buttons
// CTA 1: "Request a Quote" → /contact | CTA 2: "Explore Products" → /products
// Feature icons: Quality Certified | Bulk Export Ready | Direct from Farms | Global Shipping | Custom Packaging
// TODO: Implement full HeroSection

export default function HeroSection() {
  return <section>HeroSection placeholder</section>;
}
